# Windows_Fix
This safe, admin-run Windows Repair (.bat) script resolves system errors without deleting any personal files. It automatically detects system folders and logs all results to your Desktop. Use its simple menu to repair Windows Update, network connections, the Store, and drives, or run diagnostic scans like battery and RAM health tests.
